version: '3.8'

services:
  laboratoire_cyber:
    build: .
    container_name: mon_labo_cyber
    ports:
      # Confinement strict sur Localhost (127.0.0.1)
      - "127.0.0.1:8443:443"  # Serveur Web HTTPS
      - "127.0.0.1:2222:22"   # Serveur SSH (pour Nmap)
    restart: unless-stopped
